env: "local" # local, dev, prod
storage_path: "./storage/storage.db" # TODO: check if we use sqlite
http_server:
  address: "localhost:8082"
  timeout: 4s
  idle_timeout: 60s
  user: "testuser"
  password: "testpass"
